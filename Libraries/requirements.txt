openai>=1.14.3
tiktoken==0.5.1
python-dotenv==1.0.1
requests==2.31.0
tenacity==8.2.3
uuid==1.30

# Vector DB options (install only the one you choose to use)
# ChromaDB (local, embedded vector DB)
# chromadb==0.4.24

# Pinecone (cloud-based vector DB)
pinecone==3.0.0

# Optional (for chunking & LLM chains if needed)
# langchain==0.1.16
